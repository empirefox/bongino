import { extendObservable } from 'mobx';
import { TreeNode } from 'angular-tree-component';
import { ISite, IProfile, INav, INavItem, IPage } from 'bongin-base';

import { Level, Tree } from './structs';

export function computePage(pageNode: TreeNode): IPage {
  return
}
